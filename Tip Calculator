#Tip Calculator

print('Welcome to the tip calculator.')
total_bill = float(input("What was the total bill? "))
tip_perc = int(input("What percentage tip would you like to give? 10, 12, or 15? "))
split_num = int(input("How many people to split the bill? "))
tip = total_bill *(tip_perc/100)
pay = (total_bill + tip) /split_num
print(f"Each person should pay: {pay:0.2f}")

